{
  "scripts": {
    "dev": "concurrently \"cd backend && docker-compose up -d && npm run dev\" \"cd frontend && pnpm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  }
}
